version: "3.9"

services:

  backend:
    build: .
    container_name: edupath-backend

    ports:
      - "8000:8000"

    restart: always

  frontend:
    build: ./app
    container_name: edupath-frontend

    ports:
      - "5173:5173"

    depends_on:
      - backend

    restart: always